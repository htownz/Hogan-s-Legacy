/**
 * H2: Consolidated — this file re-exports from the primary UserContext.
 * All new code should import from "@/context/UserContext" directly.
 */
export { useUser, UserProvider } from "../context/UserContext";
