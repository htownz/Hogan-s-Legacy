export default {
  // This UI currently renders via inline styles and does not import CSS assets,
  // so we keep PostCSS local and empty to avoid inheriting the root app's CSS toolchain.
  plugins: {},
};
